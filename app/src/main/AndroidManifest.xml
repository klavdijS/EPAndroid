<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ep.cloud_store">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ep.cloud_store.activities.Home"/>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="1e63df93017cef37b9f850a5fd6e63604a2c1b1f" />

        <activity android:name="ep.cloud_store.activities.ProductDetails" />
        <activity android:name="ep.cloud_store.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ep.cloud_store.activities.RegisterActivity" />
        <activity android:name="ep.cloud_store.activities.SettingsActivity" />
    </application>

</manifest>